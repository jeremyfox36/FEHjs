<h1>All the cathcment in the database</h1>
<ul>
    <% catchments.forEach(function(item) { %>
      <li><%= item.stationnum %> <%=item.stname%></li>
      <a href="/catchments/<%= item.stationnum %>">Read More</a>
    <% }); %>
</ul>